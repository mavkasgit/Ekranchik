# Ekranchik - Справочник профилей

Flask приложение + Telegram бот для работы со справочником профилей.

## 🚀 Быстрый старт

**Запустить всё:**
```bash
start.bat
```

Откроется:
- **Web:** http://localhost:5000
- **Telegram бот:** активен (пароль: `1122`)

---

## 📋 Управление

| Файл | Команда |
|------|---------|
| `start.bat` | Запустить Docker |
| `stop.bat` | Остановить |
| `restart.bat` | Перезапустить |
| `rebuild.bat` | Пересобрать (после изменений кода) |
| `logs.bat` | Просмотр логов |
| `status.bat` | Статус контейнеров |

---

## 📱 Telegram бот

1. `/start` → запросит пароль
2. Введи `1122` → доступ открыт
3. Отправь название профиля (например: `345`, `ЮП`, `ALS-345`)

**Поиск:**
- `345` → найдет `ALS-345` (если 1 профиль - сразу с фото)
- `ЮП` → покажет список всех ЮП профилей
- `als-345` → найдет (игнорирует регистр, дефисы, Latin/Cyrillic)

**Изменить пароль:** в `.env` измени `BOT_PASSWORD=1122`

---

## 🗃️ Данные

**База данных:** `D:\KTM\Ekranchik\profiles.db` (104 профиля)  
**Фото:** `D:\KTM\Ekranchik\static\images\` (32 фото)

Всё хранится на диске - контейнер монтирует файлы напрямую.

---

## ⚙️ Настройки

Файл `.env`:
```env
TELEGRAM_TOKEN=your_bot_token
BOT_PASSWORD=1122
DB_PATH=/app/data/profiles.db
FLASK_PORT=5000
```

После изменений: `restart.bat`

---

## 🛠️ Проблемы

**Docker не запущен:**
- Запусти Docker Desktop

**Порт 5000 занят:**
```bash
netstat -ano | findstr :5000
taskkill /PID <номер> /F
```

**Нужна пересборка:**
```bash
rebuild.bat
```

---

## 📊 Структура

```
Ekranchik/
├── start.bat          # Запуск
├── stop.bat           # Остановка
├── restart.bat        # Перезапуск
├── rebuild.bat        # Пересборка
├── logs.bat           # Логи
├── status.bat         # Статус
├── app.py             # Flask сервер
├── bot.py             # Telegram бот
├── db.py              # БД
├── Dockerfile         # Docker образ
├── docker-compose.yml # Конфигурация
├── profiles.db        # База данных
└── static/images/     # Фото профилей
```

---

## 🔄 После изменений кода

```bash
rebuild.bat
```

Пересоберет контейнер с новым кодом.

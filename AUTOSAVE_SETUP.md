# 📝 Инструкция по установке автосохранения

## Проблема
Excel не сохраняет изменения автоматически → программа видит устаревшие данные (2 часа назад)

## Решение: VBA макрос автосохранения

---

## 🔧 Установка (5 минут):

**ВАЖНО:** Код нужно вставить в **2 разных места**!

### Шаг 1: Открыть редактор VBA
1. Откройте файл **"Учет КПЗ 2025.xlsm"** в Excel
2. Нажмите **Alt + F11** (откроется редактор VBA)

### Шаг 2: ЧАСТЬ 1 - Код в ThisWorkbook
1. Слева в дереве найдите **"ThisWorkbook"** (под VBAProject)
2. **Двойной клик** на "ThisWorkbook" → откроется окно кода
3. Откройте файл **`AutoSave_Macro.vba`** в блокноте
4. Найдите раздел **"ЧАСТЬ 1: КОД ДЛЯ ThisWorkbook"**
5. Скопируйте код между линиями `------------------------------------------`
6. Вставьте в окно ThisWorkbook

### Шаг 3: ЧАСТЬ 2 - Код в Module
1. В меню VBA: **Insert → Module** (создастся новый модуль)
2. В файле `AutoSave_Macro.vba` найдите раздел **"ЧАСТЬ 2: КОД ДЛЯ ОБЫЧНОГО MODULE"**
3. Скопируйте код между линиями `------------------------------------------`
4. Вставьте в окно Module

### Шаг 4: Настроить интервал
В Module найдите строку:
```vba
Public Const AUTO_SAVE_INTERVAL As Integer = 5  ' Можно изменить: 1, 5, 10, 15
```
Измените цифру на нужный интервал в минутах (например, `10` для 10 минут)

### Шаг 5: Сохранить и закрыть
1. Нажмите **Ctrl + S** (сохранить)
2. Закройте редактор VBA (Alt + Q или крестик)
3. **ВАЖНО:** Закройте и снова откройте Excel файл

### Шаг 6: Проверка
При открытии файла должно появиться сообщение:
```
✅ Автосохранение активировано!
Файл будет сохраняться каждые 5 мин.
```

---

## ✅ Как проверить что работает:

1. Откройте файл Excel
2. Введите что-нибудь в любую ячейку
3. **НЕ нажимайте Ctrl+S**
4. Подождите N минут (ваш интервал)
5. Внизу слева появится: **"💾 Автосохранение: 14:25:30"**
6. Проверьте дату файла - должна обновиться

---

## 🎮 Дополнительные команды:

После установки макроса доступны команды (Alt + F8):

- **`CheckAutoSaveStatus`** - показать статус (когда будет следующее сохранение)
- **`StopAutoSave`** - временно остановить автосохранение
- **`RestartAutoSave`** - перезапустить автосохранение

---

## 🐛 Решение проблем:

### Проблема: "Макросы отключены"
**Решение:** 
1. Файл → Параметры → Центр управления безопасностью → Параметры центра...
2. Параметры макросов → выберите "Включить все макросы" (или "Отключить все макросы с уведомлением")
3. Перезапустите Excel

### Проблема: "Макрос не запускается при открытии"
**Решение:**
- Проверьте что код `Workbook_Open()` вставлен именно в **ThisWorkbook** (ЧАСТЬ 1)
- Проверьте что функции `ScheduleAutoSave()` и другие в обычном **Module** (ЧАСТЬ 2)

### Проблема: "Файл на сетевом диске, автосохранение зависает"
**Решение:**
- Увеличьте интервал до 15-30 минут
- Или работайте с локальной копией файла, синхронизируя вручную

---

## 📊 Как это работает с нашей программой:

1. **Макрос сохраняет Excel** → обновляется `mtime` файла
2. **Watchdog ловит изменение** → сбрасывает кэш (`force_reload = True`)
3. **Следующий запрос к API** → перечитывает свежие данные
4. **Веб-интерфейс обновляется** → показывает актуальные данные

**Задержка: ~2-5 секунд** между сохранением Excel и отображением в браузере!

---

## 🔥 Рекомендуемые настройки:

- **Для активной работы:** 5 минут
- **Для фоновой работы:** 10-15 минут
- **Для тестирования:** 1 минута

---

**Нужна помощь с установкой? Напиши если что-то не работает!** 🚀
